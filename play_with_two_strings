#------------------------------------------------------------------------
# Kata Link: https://www.codewars.com/kata/56c30ad8585d9ab99b000c54
#------------------------------------------------------------------------


def swap_case(char, other_string):
    for count in range(other_string.lower().count(char.lower())):
        char = char.swapcase()
        
    return char


def work_on_strings(a,b):
    a_swapped = ''.join([swap_case(char, b) for char in a])
    b_swapped = ''.join([swap_case(char, a) for char in b])
    
    return a_swapped + b_swapped
