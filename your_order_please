#------------------------------------------------------------------------
# Date Originally Written: 08/2020
# Kata Link: https://www.codewars.com/kata/55c45be3b2079eccff00010f/python
#------------------------------------------------------------------------


import re


def order(sentence):
    if sentence == "":
        return ""
    word_list = sentence.split(" ")
    word_list = [str(re.search("\d", word).group()) + word for word in word_list]
    word_list.sort()
    return " ".join([word[1:] for word in word_list])
